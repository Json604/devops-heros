apiVersion: v1
kind: Service
metadata: {name: web-service-clusterip}
spec:
  type: ClusterIP
  selector: {app: web-clusterip}
  ports:
    - name: http
      port: 8080
      targetPort: 80
      protocol: TCP
