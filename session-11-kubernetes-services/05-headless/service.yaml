apiVersion: v1
kind: Service
metadata: {name: web-service-headless}
spec:
  clusterIP: None
  publishNotReadyAddresses: false
  selector: {app: web-headless}
  ports: [{name: http, port: 80, targetPort: 80}]
