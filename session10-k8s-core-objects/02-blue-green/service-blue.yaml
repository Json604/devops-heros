apiVersion: v1
kind: Service
metadata: {name: myapp-service}
spec:
  type: NodePort
  selector: {app: myapp, slot: blue}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30020
