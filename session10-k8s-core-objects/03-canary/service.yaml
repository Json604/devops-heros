apiVersion: v1
kind: Service
metadata: {name: myapp-canary-service}
spec:
  type: NodePort
  selector: {app: myapp-canary}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30030
