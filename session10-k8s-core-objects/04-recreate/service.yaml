apiVersion: v1
kind: Service
metadata: {name: app-recreate}
spec:
  type: NodePort
  selector: {app: app-recreate}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30040
