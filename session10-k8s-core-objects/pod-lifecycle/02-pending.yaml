apiVersion: v1
kind: Pod
metadata: {name: lifecycle-pending, labels: {app: lifecycle}}
spec:
  containers:
    - name: impossible
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
      resources:
        requests: {memory: 1000Gi}
