apiVersion: v1
kind: Pod
metadata: {name: lifecycle-succeeded, labels: {app: lifecycle}}
spec:
  restartPolicy: Never
  containers:
    - {name: done, image: busybox:1.36, command: ["sh", "-c", "echo succeeded"]}
